#include <iostream>
#include <vector>
#include <string>
#include <unordered_map>
using namespace std;

int main()
{
    string ss = "a friend in need is a friend indeed";
    /*
    // cin >> ss;
    unordered_map<string, int> mapp;

    vector<string> vv;
    int i=0;
    int len = ss.size();
    while(i<len){

    }

    while(){

        string cur = ss[i];
        if(mapp.find(cur) == mapp.end()){
            mapp[cur] = 1;
        }
        else{
            ++mapp[cur];
        }

    }


    for(auto &m : mapp){
        Node node(m.first,m.second);
        if(index != topk){
            heap[index] = node;
            heapInsert(heap,index++);
        }
        else{
            if(heap[0].times<node.times){
                heap[0] = node;
                adjust(heap,0,topk);
            }
        }
    }
*/
int a,b;
cin >> a >> b;
    cout << a << " "<< b  << endl;
    return 0;
}
